{
  "name": "typescript-lambda-bundle",
  "version": "1.0.0",
  "description": "Example of a TS lambda",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p .",
    "postbuild": "NODE_ENV=production parcel build dist/index.js --out-dir dist",
    "prebuild": "rimraf dist"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.39",
    "@types/node": "^13.1.0",
    "eslint": "^6.8.0",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0",
    "ts-node": "^8.5.4",
    "tslint": "^5.20.1",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "aws-sdk": "^2.596.0",
    "source-map-support": "^0.5.16"
  }
}
